Simply go into client1 and service1 and do npm install in each.
Afterwards start the service and then run the client.

```
cd service1 && npm install && npm start &
cd client1 && npm install && npm start
```